# 인수인계: 「둘 중 하나」 블록 퍼즐 게임

> 새 PC에서 Claude에게 이 문서를 통째로 붙여넣고 "이어서 하자"라고 하면 됩니다.
> 게임 폴더(`index.html` + `icons/` + `fonts/` + `manifest.json` + `sw.js`)를 통째로 옮기면 됩니다.

## 0. 어디에 있나
- **작업 폴더:** `C:\Users\user\Desktop\둘중하나`

  | 파일 | 뭔지 |
  |---|---|
  | `index.html` | **게임 본체.** CSS/JS 전부 한 파일. 외부 라이브러리 없음 |
  | `manifest.json` | 앱 이름·아이콘·세로고정 (홈 화면 추가용) |
  | `sw.js` | 서비스 워커 — 오프라인 실행용 캐시 |
  | `icons/` | 앱 아이콘 5종 (192·512·maskable·apple-touch·favicon) |
  | `fonts/` | 내장 폰트 woff2 2개 + `fonts.css` (합계 62KB) |
  | `tools/make-icons.py` | 아이콘 생성기 — 색·모양 바꾸고 다시 돌리면 됨 |
  | `tools/build-fonts.py` | 폰트 다운로더 |
  | `dul-jung-hana.backup.html` | 보관 기능 넣기 전 옛 버전 |
  | `.claude/launch.json` | 로컬 서버 설정. Claude에게 "게임 띄워줘" 하면 실행됨 |

  - 파일 이름이 `dul-jung-hana.html` → **`index.html`** 로 바뀜. 배포했을 때 주소가 `.../dul-jung-hana/` 로 깔끔해지고 PWA `start_url` 도 `./` 로 단순해지기 때문.
- **배포 주소 (GitHub Pages):** https://hn1022.github.io/dul-jung-hana/
  - 저장소: https://github.com/HN1022/dul-jung-hana (Public)
  - `main` 브랜치에 push하면 1~2분 뒤 자동 반영됨. 따로 배포 명령 없음.
  - 친구들은 이 링크를 열고 브라우저 메뉴 → "홈 화면에 추가" 하면 앱처럼 설치됨.
- **테스트할 때 주의:** `file://` 이나 Artifact 미리보기에서는 **localStorage가 막혀서 저장·이어하기가 동작하지 않음.** 반드시 `http://localhost:8123` 으로 띄워서 확인할 것.
- **Artifact 링크 (v8 — 지금은 옛날 버전):** https://claude.ai/artifact/8n3FUP3Qc81aSaSsgLiRZU
  - 로컬 파일이 더 최신임. 이 링크를 갱신하려면 Claude에게: `Artifact` 도구로 이 URL을 `action: "read"` 한 뒤 `url`을 지정해 publish (url 없이 publish하면 새 링크가 생김).

## 1. 최종 목표
- 인터넷 없이 할 수 있는, **단순하지만 중독성 있는 모바일 게임 앱**.
- 사용자는 2048·수박게임 같은 합치기/퍼즐 류를 좋아함 → 직접 이 게임 규칙을 기획함.

## 2. 현재 게임 규칙 (구현 완료)
1. **크기 선택:** 모드 범위 안의 블록 크기 두 개가 나오고, 하나를 고름.
2. **모양 선택:** 그 크기의 폴리오미노가 무작위로 2개(서로 회전으로 겹치지 않는 모양) 나옴.
3. **놓기** — 조작 방식 2가지 (헤더의 ✏️/✋ 버튼 한 번으로 전환, 마지막 선택 기억)
   - ✏️ **그리기**: 보드 빈칸을 탭/드래그로 칠함. 두 모양 중 하나와 **회전까지만** 일치하면 손 뗄 때 놓임. **뒤집기(거울상)는 인정 안 함 — 사용자가 명시적으로 원함.**
   - ✋ **끌어놓기**: 탭 또는 ↻ 버튼으로 회전, 드래그로 배치. PC는 드래그 중 R키 회전.
     - **카드 전체가 드래그 히트박스** (블록 그림 위가 아니어도 됨). ↻·📦 버튼에서 시작할 때만 제외 — `armDrag()` 참고.
4. **줄 삭제:** **가로 줄만** 삭제 (세로 삭제는 사용자 요청으로 제거). 삭제되면 **위 블록들이 아래로 내려옴**(테트리스식, 줄 단위).
5. **게임 오버:** 손에 있는 블록(제시된 것 + 보관함) 전부 어떤 회전으로도 놓을 곳이 없을 때.
6. **📦 보관 (힌트를 대체함 · 결제 요소 자리):**
   - 블록 카드의 📦 버튼을 누르면 그 블록이 보관함으로 들어감.
   - **보관하면 그 턴은 끝남** — 아무것도 놓지 못하고 크기 선택부터 다시 시작. 방해 블록 카운트는 그대로 진행됨(`afterPlacement()` 호출).
   - 보관된 블록은 다음 턴부터 제시된 블록과 **똑같이** 놓을 수 있음(그리기·끌어놓기·회전 전부). 놓으면 칸이 비워짐.
   - **슬롯은 총 2칸: 무료 1칸 + 잠긴 1칸(🔒).** 잠긴 칸은 **1회 결제 2,000원**으로 여는 것이 계획. 지금은 누르면 "준비 중이에요" 알림만 뜸.
   - 조절용 상수: `MAX_SLOTS = 2`(화면에 보이는 칸 수), `FREE_SLOTS = 1`(무료 칸).
   - **결제 붙일 때:** 결제 성공 후 `window.unlockSlots(2)` 한 번 호출하면 끝. localStorage 키 `duljunghana-v3-slots`에 저장돼서 다음에도 유지됨.
7. **점수:** 블록 칸 수 + 줄 보너스 `10 × 줄수² × 연속콤보` → 난이도 배율 곱함.

## 2-1. 화면 구성 (2026-09-17 개편)
- **시작 화면**(`#startScreen`): 크기 모드 · 난이도 · 조작 방식을 여기서 고름. 선택한 조합의 최고 기록이 바로 보이고, 저장된 판이 있으면 `이어서 하기` 버튼이 나타남.
  - 규칙 설명도 여기 `<details>` 안으로 옮김(게임 화면에서는 안 보임).
  - 새로고침하면 항상 시작 화면부터. (Artifact 핫리로드 때는 하던 판 그대로 유지)
- **게임 화면 헤더는 한 줄뿐:** `[☰ 3~5칸 · Lv1] [✏️] ........ [점수] [최고]`
  - 폰에서 점수가 안 보인다는 피드백 때문에 툴바 2줄을 없애고 점수 글자를 키움(30px).
  - ☰ 를 누르면 저장하고 시작 화면으로 감. ✏️/✋ 는 누를 때마다 조작 방식이 바뀜.
- 보드 아래: 스텝 안내 → 선택 카드 2장 → **📦 보관함 줄**(무료 1칸 + 🔒 1칸).

## 3. 모드 / 난이도 / 확률 (조절용 숫자)
- **크기 모드:** `3~5칸`, `1~6칸` (보드 8×8)
- **난이도 1~4 (방해 블록 = 회색 빗금 칸, 빈칸 2개 이상인 줄에만 떨어짐 → 공짜 줄 완성 없음)**

  | 단계 | 방해 블록 | 점수 배율 | 큰 블록 쏠림(LEVEL_PUSH) |
  |---|---|---|---|
  | 1 | 없음 | ×1 | 0 |
  | 2 | 4턴마다 1칸 | ×1.5 | 0.4 |
  | 3 | 3턴마다 2칸 | ×2 | 0.8 |
  | 4 | 2턴마다 2칸 | ×3 | 1.2 |

- **크기 확률:** `가중치 = BASE_WEIGHTS × exp(push × 크기순위)`
  - `BASE_WEIGHTS`: 3~5칸 = 모두 1, 1~6칸 = `{1:1, 2:2, 3:4, 4:6, 5:7, 6:7}`
  - `push = LEVEL_PUSH[단계] + GROWTH_MAX(1.0) × min(1, 턴/GROWTH_TURNS(80))`
  - 20턴마다 "블록이 점점 커져요" 알림, 80턴에 최대치.
- 모양 확률: 뒤집기 미인정 기준 서로 다른 조각(4칸 7종, 5칸 18종, 6칸 60종)을 균등하게 뽑음. 3칸은 원래 2종(일자, ㄱ자)뿐.
- 과거 버그 수정: 크기 섞기를 `sort(() => Math.random()-0.5)`로 해서 치우쳤던 것 → Fisher–Yates/가중 추첨으로 교체함.

## 4. 저장 (localStorage)
- 키 접두어 `duljunghana-v3-`
- 진행 중인 판 + 최고기록: 크기모드×난이도별 슬롯 (`…35`, `…16`, `…35-L2` 등. 1단계는 접미사 없음)
  - 저장 내용에 `held`(보관함) 추가됨. 옛 저장의 `hintUsed`는 그냥 무시되므로 기존 기록은 그대로 이어짐.
- 마지막 모드/난이도/조작방식, 그리고 `…-slots`(열린 보관 칸 수)도 저장.

## 4-1. PWA (2026-09-17 완료)
- 홈 화면에 추가하면 아이콘·전체화면·세로고정·오프라인 실행까지 다 됨. 확인 완료(서버 꺼놓고 새로고침해도 정상 실행).
- **폰트를 파일로 내장함.** 게임에 실제로 쓰인 글자만 골라 받는 서브셋이라 62KB밖에 안 됨.
  - ⚠️ **한글 문구를 새로 추가하면 `python tools/build-fonts.py` 를 다시 돌릴 것.** 안 돌리면 새 글자만 시스템 폰트로 나옴(깨지진 않음).
  - 구글 폰트는 이제 변수 폰트라 굵기별 파일이 전부 같음 → 패밀리당 파일 1개 + `font-weight: 400 900` 범위 지정.
- **캐시 전략:** 게임 화면(HTML)은 네트워크 먼저(고친 게 바로 반영됨), 폰트·아이콘은 캐시 먼저.
  - `sw.js` 의 `ASSETS` 목록을 고쳤으면 **`VERSION` 을 올릴 것.** 안 올리면 옛 캐시가 안 지워짐.
- **아이콘:** 블록 두 개 중 주황색(고른 것)이 크고, 회색(안 고른 것)이 작음. `tools/make-icons.py` 의 상수만 고치고 다시 돌리면 5종이 한 번에 다시 만들어짐 (PIL 같은 라이브러리 필요 없음).

## 4-2. 안드로이드 앱 (2026-09-18, Capacitor 8.5.2)
- **테스트용 APK 만들기: `npm run apk`** → 프로젝트 루트에 `둘중하나-debug.apk` 생성 (git에는 안 올라감)
- 앱 ID **`com.alwaysone.duljunghana`** — 플레이스토어에 올리면 변경 불가
- compile/target SDK 36, 최소 SDK 24(Android 7). 세로 고정, 진동 권한.
- 아이콘·시작 화면은 `python tools/make-icons.py` 한 번으로 웹+안드로이드 전부 다시 생성됨.
- 웹 파일은 루트에 그대로 두고(GitHub Pages용), 앱 빌드 때만 `www/`로 복사해서 넣음(`tools/build-web.mjs`). **게임에 새 파일을 추가하면 `build-web.mjs`의 `FILES` 목록에도 추가할 것.**
- 앱 안에서는 서비스 워커를 등록하지 않음(`window.Capacitor` 체크).
- **이 PC 개발 환경:** Node 24.19 / Android SDK (`%LOCALAPPDATA%\Android\Sdk`: platform 36, build-tools 36.1) / Java 21 (`%LOCALAPPDATA%\Programs\jdk-21…`, 사용자 `JAVA_HOME`).
- ⚠️ **폴더 이름에 한글(둘중하나)이 있어서 생긴 우회 두 가지:**
  - `android/gradle.properties`에 `android.overridePathCheck=true` (안 넣으면 빌드 자체를 거부함)
  - `cmd`가 한글 경로를 못 읽어서 `gradlew.bat`이 안 됨 → `build-apk.mjs`가 cmd 없이 Java로 Gradle을 직접 실행.
  - 나중에 빌드가 이상하게 깨지면 폴더를 영문 경로(예: `C:\dev\dul-jung-hana`)로 옮기는 게 근본 해결.
- Gradle 8.14.3은 Java 25(Android Studio 내장 jbr)로 못 돌아서 Java 21을 따로 설치함.

## 4-3. 광고·결제·영어 (2026-09-18) — 코드는 완료, 실제 결제/광고는 아직 테스트 광고

**수익 구조 (확정)**
| | 안드로이드 앱 | 웹 (아이폰 등) |
|---|---|---|
| 보관 칸 | 1칸 무료 + **"보관 칸 +1" 월 자동갱신 구독 ₩1,300** | 2칸 무료 |
| 광고 | 하단 배너 + 게임오버 후 전면광고(2판에 1번) | 없음 |
| 광고 제거 | **평생 1회 결제 ₩3,900** (배너·전면 둘 다 제거) | — |

- 웹 애드센스는 보류: github.io 하위 주소라 승인 어렵고, 트래픽 적으면 수익 미미. 방문자 늘면 도메인 사서 재검토.
- 구글 정책: 앱 안에서 "웹에서 무료로 하세요" 같은 안내 금지.

**파일**
- `money.js` — 광고(AdMob)·결제(Google Play) 전부. 웹에서는 아무것도 안 함. 맨 위 `CONFIG`에 ID·빈도.
- `i18n.js` — 한국어/영어 문구 사전. 시작 화면 제목 옆 🌐 버튼으로 전환(처음엔 폰 언어를 따름). 영어 게임 이름은 임시로 **"Pick One"**.
- 플러그인: `@capacitor-community/admob` 8.1.0, `@capgo/native-purchases` 8.7.0 (별도 서비스 가입 없이 구글 결제 직접).
- 보관 칸 구독이 끝나도, 이미 두 번째 칸에 들어 있던 블록은 꺼내 쓸 수 있음(잠금은 빈 칸에만).
- 상점: 시작 화면 "🛒 상점" 버튼 또는 잠긴 🔒 보관 칸을 누르면 열림. 가격은 구글에서 받아와 표시(코드에 가격 없음).
- 상태바에 점수가 가리던 문제 → `--top-gap`(안드로이드 `--safe-area-inset-top` / 아이폰 `env()`) 만큼 위 여백. 배너 높이만큼 아래 여백(`--ad-h`).

**⚠️ 광고·결제 출시 체크리스트 (신원 확인 끝난 뒤)**
1. **AdMob 계정** 만들기(admob.google.com) → 앱 등록 → 광고 단위 2개(배너, 전면) 생성
2. 받은 ID로 교체:
   - `android/app/src/main/AndroidManifest.xml` 의 `com.google.android.gms.ads.APPLICATION_ID` (지금 `ca-app-pub-3940256099942544~3347511713` = 테스트)
   - `money.js` `CONFIG.bannerId`, `CONFIG.interstitialId`, 그리고 `adsTesting: false`
   - ⚠️ 테스트 ID인 채로 출시하면 수익 0. 반대로 **실제 ID로 개발 중에 본인이 광고를 누르면 AdMob 계정 정지** 위험 → 실제 ID 넣은 뒤엔 본인 폰을 테스트 기기로 등록
3. Play Console에서 앱 만들기 → **출시용 서명 키로 AAB 빌드** → 내부 테스트 트랙에 업로드 (결제 권한이 들어간 빌드를 한 번 올려야 상품 등록이 열림)
4. 상품 등록 — **ID를 코드와 똑같이**:
   - 인앱 상품 `remove_ads` (일회성) ₩3,900
   - 구독 `slot_monthly`, 기본 요금제 ID `monthly` (월간 자동 갱신) ₩1,300
5. 판매자 프로필(수익 받을 계좌·세금 정보) 설정
6. Play Console "라이선스 테스트"에 본인 구글 계정 추가 → 실제 돈 안 나가는 테스트 결제
7. 앱 콘텐츠 신고: **광고 포함 = 예**, 데이터 보안 양식(AdMob이 광고 ID 수집), **개인정보처리방침 URL 필수**(GitHub Pages에 페이지 하나 만들면 됨), 타깃 연령(어린이 포함하면 광고 제한이 커지니 13세 이상 권장)
8. `app-ads.txt` — AdMob이 요구. 스토어 등록정보의 개발자 웹사이트 루트에 둬야 함 (github.io면 `HN1022.github.io` 저장소가 따로 필요)

**아직 확인 못 한 것:** 실제 폰에서 배너 위치·전면광고·결제창. (브라우저에서 가짜 결제 모듈로 흐름만 검증: 초기화→배너→전면 미리 로드→광고 제거 구매→배너 제거→"사용 중 ✓")

## 5. 주의 / 확인된 것
- **2026-09-17에 localhost로 띄워 실제 플레이 확인 완료:** 블록 놓기, 가로 줄 삭제 + 낙하, 점수/콤보, 방해 블록, 보관 → 다음 턴에 꺼내 놓기, 시작 화면, 새로고침 후 이어서 하기, 드래그 히트박스까지 전부 정상.
- 게임 오버 화면(`메뉴` / `한 판 더` 두 버튼)은 코드로만 확인했고 실제로 끝까지 가보진 않음.
- 뷰포트 메타 태그가 없어서 폰에서 축소돼 보이던 문제 수정함(Artifact 뷰어가 가려주고 있었음). 이제 파일 맨 위에 `charset` · `viewport` · `color-scheme` 메타가 있음.
- 코드에 `window.claude?.hot` 관련 코드가 있음 (Artifact 뷰어용 상태 유지). 앱으로 옮길 때는 없어도 되고, 있어도 무해함.

## 6. 다음 할 일 (합의된 추천 순서)
1. ~~프로젝트 폴더 만들고 게임 파일 넣기~~ (완료)
2. ~~**PWA로 만들기 + 배포**~~ (완료 — https://hn1022.github.io/dul-jung-hana/ 에서 돌아감)
   - **남은 것: 친구들에게 링크 보내서 반응 보기.**
   - 고친 걸 반영하려면 `git add -A && git commit -m "..." && git push` 만 하면 됨.
3. 반응 좋으면 **Capacitor로 안드로이드 앱** (Windows에서 가능)
   - Node.js + Android Studio 설치 → APK를 폰에 설치해 테스트
   - 플레이스토어: 구글 개발자 계정(1회 약 $25), 신규 개인 계정은 비공개 테스트 기간 필요
   - 아이폰은 Mac + 연 $99 필요
4. 앱화할 때 같이: 세로 고정, 진동 강화, 아이콘/스플래시 화면.
5. **보관 칸 결제 붙이기** (아직 안 함): 두 번째 보관 칸을 **1회 결제 2,000원**으로 판매할 계획. 결제 성공 시 `window.unlockSlots(2)` 호출만 하면 됨.
   - 비용: PWA는 0원 / 구글 플레이 개발자 등록 1회 $25(신규 개인 계정은 테스터 12명 · 14일 비공개 테스트 필요) / 애플은 연 $99 + 맥 필요.
   - 인앱결제 수수료는 구글·애플 모두 15%(연매출 100만 달러 이하), 초과분 30%.

## 7. 사용자 스타일 메모
- 한국어로 짧게 대화. 아이디어를 바로바로 던지고 빠르게 결과 보는 걸 선호("ㄱㄱㄱ" = 제안한 거 다 진행).
- 수치(확률, 간격 등)는 Claude가 정해서 제안하되, 조절 가능한 숫자를 알려주는 걸 좋아함.

---

## 2026-09-18 추가 작업 (회사 PC, GitHub 클론에서 작업)
- **확대 차단**: 보드를 여러 번 두드리면 화면이 확대되던 문제 수정. `viewport`(maximum-scale=1, user-scalable=no) + `body { touch-action: manipulation }` + 사파리용 `gesture*`/`dblclick` preventDefault.
- **영어 이름 확정: `Either Or`** (임시 이름 Pick One 교체). i18n.js 의 en.doc_title / en.title 수정. 스토어 등록명은 `Either Or: Block Puzzle` 형태 추천.
- **개인정보처리방침 추가**: `privacy.html` (한국어 + 영어 한 페이지). 시작 화면 맨 아래 링크, sw.js 캐시 목록에도 추가(VERSION v4).
  - URL: https://hn1022.github.io/dul-jung-hana/privacy.html
  - 문의 이메일: `either21or@gmail.com` (앱 전용 계정. AdMob·Play Console 도 이 계정으로 통일할 것)
  - 개발자 표기는 `Always One` 로 통일. 다른 이름으로 낼 거면 같이 수정.
- 남은 일은 위 "4-3" 체크리스트 그대로 (AdMob 실제 ID 교체, Play Console 내부 테스트, 상품 2개 등록).
- APK 는 이 PC(Node.js·Android Studio 없음)에서 빌드 못 함 → 집 PC 에서 다시 빌드해야 위 수정들이 앱에도 반영됨.

### AdMob / Play Console 진행 상황 (2026-09-18)
- **Play Console**: 계정 `wnwkr2348@gmail.com`, 개발자 이름 `Always One`, **개인 계정**. 등록비 결제 완료.
  - 상태: **신원 확인 진행 중**(문서 업로드됨, 며칠 소요) → 확인 끝나야 전화번호 인증·앱 만들기 가능.
  - `either21or@gmail.com` 을 **관리자(모든 권한)** 로 초대함(2026-09-18, 초대 전송됨 → 수락 필요).
- **AdMob** (계정: `either21or@gmail.com`): 앱 `둘 중 하나`(Android) 추가, 광고 단위 2개 생성.
  - 앱 ID: `ca-app-pub-4373923440824013~2333478234` → AndroidManifest.xml
  - 배너: `ca-app-pub-4373923440824013/2784517335` → money.js
  - 전면: `ca-app-pub-4373923440824013/9158353992` → money.js
  - `adsTesting: false` 로 바꿈. **내 폰에서 광고를 누르지 말 것**(무효 클릭 → 계정 정지 위험). 테스트가 필요하면 AdMob 에서 테스트 기기를 등록할 것.
  - AdMob 앱 상태: **"검토 필요 · 광고 게재 제한됨"** — 플레이스토어에 앱을 게시하고 AdMob 앱에 "스토어 추가"로 연결하면 풀린다. 그때까지는 광고가 안 나올 수 있음(정상).
  - AdMob 결제/세금 정보는 아직 입력 안 함 → 수익 지급 받으려면 필요.

### 테스트 APK 자동 빌드 (GitHub Actions, 2026-09-18~)
- `main` 에 푸시하면 GitHub 가 테스트 APK 를 만들어 `apk` 브랜치에 덮어씀. 링크는 그대로:
  https://github.com/HN1022/dul-jung-hana/raw/apk/dul-jung-hana-test.apk
- 설정 파일: `.github/workflows/apk.yml`. 진행 상황은 GitHub 저장소 → Actions 탭. 손으로 돌리려면 Actions → "테스트 APK 빌드" → Run workflow.
- **서명 키 통일**: `android/app/ci-debug.keystore`(테스트 전용, 비밀번호 android). 첫 자동 빌드 때 생성돼 저장소에 올라감. `build.gradle` 의 `signingConfigs.debug` 가 이 키를 쓰므로 집 PC `npm run apk` 도 같은 키 → 어디서 빌드해도 폰 앱 위에 업데이트됨.
  - 단, 이 키로 바뀌기 **전에** 집 PC 에서 깔았던 앱은 키가 달라서 **한 번은 지우고 설치**해야 함.
  - ⚠️ 출시용(플레이스토어) AAB 는 이 키 말고 별도 업로드 키로 서명할 것.
- `tools/build-web.mjs` 에 `privacy.html` 추가 (앱 안 개인정보처리방침 링크가 깨지지 않게).

### 온라인 순위표 (2026-09-18)
- **구조**: `engine.js`(게임 규칙 — 화면과 검증이 같이 씀) · `leaderboard.js`(Firebase REST, 익명 로그인) · `firestore.rules`(1단계) · `tools/verify-scores.mjs`(2단계) · `admin.html`(3단계)
- **Firebase**: 프로젝트 `either-or` (ID `either-or-130af`, 계정 `either21or@gmail.com`), Firestore 서울(asia-northeast3), 익명 + Google 로그인, 승인 도메인 `hn1022.github.io`. Analytics·Gemini 끔.
- **리플레이**: 판마다 seed 로 무작위를 고정하고 모든 행동을 `G.log` 에 기록 → `Engine.replay(meta, log)` 로 같은 점수 재현. ⚠️ 규칙(점수·확률·방해 블록 등)을 바꾸면 `engine.js` 의 `VERSION` 을 올릴 것(검증기는 다른 버전 기록을 건드리지 않음).
- **1단계** `firestore.rules`: 본인 문서(`uid_모드_난이도`)만, 정해진 칸만, 닉네임 2~10자, 턴 수 기준 점수 상한, 더 높은 점수로만·10초 간격, status 는 pending 만. 삭제는 either21or 만. (브라우저에서 상한 초과·남의 문서·1자 닉네임·스스로 ✓ → 전부 403 확인)
- **2단계** `.github/workflows/firebase.yml`: 매일 03:00 KST + firestore.rules/engine.js/tools 변경 시. 리플레이 일치 → status "ok"(✓), 불일치 → 삭제.
- **인증(키 없음)**: Workload Identity. 풀 `github` / 제공자 `github-actions`(조건: `assertion.repository == 'HN1022/dul-jung-hana'`) → 서비스 계정 `github-actions@either-or-130af.iam.gserviceaccount.com`(역할: Cloud Datastore 사용자, Firebase 규칙 관리). 비밀 키 파일은 만들지 않았음.
  - firebase-admin 은 이 자격증명을 못 읽어서 검증은 `@google-cloud/firestore` 를 씀.
- **3단계** https://hn1022.github.io/dul-jung-hana/admin.html — either21or 구글 로그인 시에만 삭제 가능.
- 엔진 도입 전에 시작한 판은 `unranked`(이어서는 하되 순위 등록 불가).
- 개인정보처리방침에 순위표 항목 추가함. **Play Console "데이터 보안"에 닉네임·게임 기록 수집 기재 필요.**
